<!--
Thanks for opening a PR.

Reference the linked issue in the body so it auto-closes on merge to main
(or, for PRs into qa, so the qa -> main promotion PR aggregates it):

  Closes #N

The `## Acceptance criteria covered` section below is the convention introduced
after #1259 / #1260, where partial-scope changes shipped because the
orchestrator's brief dropped criteria from the issue body and no later step
re-read the issue. Filling it in lets the `code-reviewer` sub-agent verify
coverage against the linked issue at PR open. See AGENTS.md
"Sub-agents and orchestration" for the full convention.
-->

## Summary

<one or two sentences>

## Acceptance criteria covered

<!--
List every criterion from the linked issue's body. Use:
  - [x] criterion implemented by this PR
  - [ ] criterion deferred — <reason + follow-up issue link>

If the change is trivial (no linked issue, or the issue body has no
acceptance-criteria section), state "N/A — trivial change" instead.
-->

- [ ]

## Test plan

- [ ]
