// Stub type for the Supabase database schema.
// Replace with the output of `supabase gen types typescript --project-id <ref>`
// after the migration in db/migrations/001_initial_sync_schema.sql has been applied.
export type Database = unknown;
