---
kind: changed
---
- Progress bars on the Stats, Journey, and Settings pages now emit consistent accessible meter semantics (`role="meter"` with `aria-valuenow`/`aria-valuemin`/`aria-valuemax` and an accessible label), improving the experience for screen-reader users.
