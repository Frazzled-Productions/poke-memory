---
kind: fixed
---
- CI agents can no longer escape into the `fewer-permission-prompts` skill when a permission denial fires; `--disable-slash-commands` is now passed to all 12 `claude-code-action@v1` invocations across 10 workflow files.
