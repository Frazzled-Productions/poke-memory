---
kind: fixed
---
- Monitoring divergence check: Option B now applies the same 2-day persistence window as Option A, so re-learning sessions after a pull-normalisation reset no longer trigger spurious "row stuck stale" alerts.
