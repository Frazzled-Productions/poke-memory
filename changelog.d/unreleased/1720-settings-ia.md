---
kind: minor-bump
---

Settings page reorganised into 10 focused sections. Account/sync status now visible above the accordions at all times; Language promoted from Labs to a top-level section; Card types split from Practice schedule with cry-enable relocated there; About, Regional & reminders, and Data & backup each have their own section. Sub-section headings are real `<h3>` elements for screen-reader navigation. Card types defaults open on first visit. Settings search index includes language endonyms.
