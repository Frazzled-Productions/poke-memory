// Twitter/X cards use the same 1200×630 design as the OpenGraph image.
// Re-exporting from the shared implementation keeps them in sync.
export { alt, size, contentType, default } from "./opengraph-image";
