/**
 * @deprecated Import `addOnboardingPreDismiss` from `./onboarding` instead.
 * This re-export exists only to avoid a large churn commit while callers
 * migrate. It will be removed once all callers have been updated.
 */
export { addOnboardingPreDismiss as preDismissOnboardingModal } from "./onboarding";
